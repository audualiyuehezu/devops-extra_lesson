#!/bin/bash
# This a test of microphonei
echo "Hello world" # This is also a comment

#variable a container that stores data
# in unix we have system variable and user define variable
#system :-created and maintain by unix they are define in capitall cases
#user-define variable:- There usually written in lowercase globally, it bas on choice


echo Our shell name is $BASH
echo Our shell version name is $BASH_VERSION
echo Our home directory is $HOME
echo Our current working directory $PWD

#user-define variable

name=Mark
echo The name is $name 

val=10

echo The name is $name
echo value $val
